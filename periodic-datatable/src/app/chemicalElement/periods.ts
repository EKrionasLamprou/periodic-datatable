import { Period } from "./period";

export class Periods {
    /**
     * A list of all chemical element periods.
     */
    static list: Period[] = [
        {
            index: 1,
            description: 'The first period contains fewer elements than any other, ' +
                'with only two, hydrogen and helium. They therefore do not follow the ' +
                'octet rule, but rather a duplet rule. Chemically, helium behaves like a ' +
                'noble gas, and thus is taken to be part of the group 18 elements.',
        },
    ]
}